import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'PDF to Text Converter Online Free | SelfPDF', description: 'Extract copyable text from PDF files online with SelfPDF.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
