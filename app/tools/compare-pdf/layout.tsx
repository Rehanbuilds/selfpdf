import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Compare PDF Files Online Free | SelfPDF', description: 'Compare two PDF documents and review page differences with SelfPDF.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
