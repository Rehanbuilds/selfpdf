import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Delete PDF Pages Online Free | SelfPDF', description: 'Remove unwanted pages from PDF files online and download a clean copy.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
