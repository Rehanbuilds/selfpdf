import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Redact PDF Online Free | SelfPDF', description: 'Prepare PDF documents for privacy review and redaction with SelfPDF.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
