import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Free PDF Tools Online | SelfPDF', description: 'Use SelfPDF tools to merge, split, compress, convert, sign, and organize PDF files in your browser.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
